#include <iostream>
using namespace std;

int main(){
    char Employee_code;
    int income_type;
    double state_code, tax_rate;


    if (Employee_code == 'A'){
        if (state_code == 'Y'){
            tax_rate = 0.080;
        }
            else {
            tax_rate = 0.650;
            }
    } 
    else {
        if (income_type == 0){
            tax_rate = 0.030;
        }
            else {
                tax_rate = 0.020;
            }
    }
    return 0;
}

